rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ─── Helper Functions ────────────────────────────────────────
    
    // Check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // Check if user owns the document
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // Note: avoid using get() in rules for list/query safety. Rely on document fields like userId instead.

    // ─── users Collection ────────────────────────────────────────
    // Users can only read/write their own documents
    match /users/{userId} {
      allow read: if isAuthenticated() && isOwner(userId);
      allow write: if isAuthenticated() && isOwner(userId);
      allow create: if isAuthenticated() && isOwner(userId);
      allow delete: if isAuthenticated() && isOwner(userId);
      
      // Health Checks subcollection
      match /healthChecks/{checkId} {
        allow read, write, create: if isAuthenticated() && isOwner(userId);
      }
    }

    // ─── sessions Collection ────────────────────────────────────────
    // Users can only access sessions they own
    match /sessions/{sessionId} {
      // Allow single-document get when the session belongs to the user
      allow get: if isAuthenticated() && resource.data.userId == request.auth.uid;

      // Allow list queries for authenticated users (frontend filters by userId)
      allow list: if isAuthenticated();

      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if isAuthenticated() && resource.data.userId == request.auth.uid;
    }

    // ─── documents Collection ────────────────────────────────────────
    // Users can only access documents in their sessions
    match /documents/{docId} {
      // Documents include `userId` when created; allow access based on that field to avoid get() in rules
      allow read: if isAuthenticated() && resource.data.userId == request.auth.uid;
      allow write: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow delete: if isAuthenticated() && resource.data.userId == request.auth.uid;
    }

    // ─── alerts Collection ──────────────────────────────────────────
    // Users can only access their own alerts
    match /alerts/{alertId} {
      allow read: if isAuthenticated() && 
                     resource.data.userId == request.auth.uid;
      allow write: if isAuthenticated() && 
                      request.resource.data.userId == request.auth.uid;
      allow create: if isAuthenticated() && 
                       request.resource.data.userId == request.auth.uid;
      allow delete: if isAuthenticated() && 
                      resource.data.userId == request.auth.uid;
    }

    // ─── library Collection ─────────────────────────────────────────
    // Library articles are public, anyone can read
    // Only admins/backend can write (via Firebase Admin SDK)
    match /library/{articleId} {
      allow read: if true;
      allow write: if false;  // Only admin SDK can write
      allow create: if false; // Only admin SDK can create
      allow delete: if false; // Only admin SDK can delete
    }

    // ─── outcomes Collection ────────────────────────────────────────
    // Only backend can write (via Admin SDK) -> Actually, frontend writes outcomes now
    // Users cannot read outcomes of others
    match /outcomes/{outcomeId} {
      allow read: if false;   // Analytics only via backend
      allow write, create: if isAuthenticated(); // Frontend writes outcomes now
      allow delete: if false; // Only admin SDK can delete
    }

    // ─── Default Deny ───────────────────────────────────────────────
    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
